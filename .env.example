# Phantom State MCP Configuration
# Copy this to .env and customize as needed

# Database path (relative or absolute)
PHANTOM_DB_PATH=narrative.db

# Embedding backend: "local", "openai", or "hash" (deterministic, dependency-free)
PHANTOM_EMBEDDING_BACKEND=local

# Local embedding model (used if backend=local)
PHANTOM_EMBEDDING_MODEL=all-MiniLM-L6-v2

# OpenAI embedding model (used if backend=openai)
PHANTOM_OPENAI_MODEL=text-embedding-3-small

# Vector dimensions (must match your embedding model)
# all-MiniLM-L6-v2: 384
# text-embedding-3-small: 1536
PHANTOM_VECTOR_DIMENSIONS=384

# OpenAI API key (required if backend=openai)
# OPENAI_API_KEY=sk-...
